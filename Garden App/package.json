{
  "name": "codegarden.app",
  "version": "0.1.0",
  "dependencies": {
    "three": "^0.116.1"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "marked": "^1.1.0"
  },
  "scripts": {
    "dev": "parcel build src/static/**/* --out-dir dist/static & parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./ --out-dir ./../../codegarden-2020-group-image-gh-pages & parcel build src/static/**/* --out-dir ./../../codegarden-2020-group-image-gh-pages/static"
  }
}
